# ══════════════════════════════════════════════════════════════════════════════
# workspaces.conf  –  Workspace assignments and switching keybindings
# Mirrors:  workspaces.conf  (Hyprland)
#
# Your Hyprland layout used numbered workspaces 10-18 with different layout
# types per workspace. In scroll there is only ONE layout (scrolling). The
# layout can be oriented as horizontal or vertical per workspace via the
# `set_mode h|v` command, which persists per workspace until you change it.
#
# Workspace renumbering:
#   Hyprland WS 10 → scroll WS 1  (scrolling horizontal – default)
#   Hyprland WS 11 → scroll WS 2  (scrolling horizontal)
#   Hyprland WS 12 → scroll WS 3  (scrolling horizontal)
#   Hyprland WS 13 → scroll WS 4  (horizontal mode – was dwindle)
#   Hyprland WS 14 → scroll WS 5  (horizontal mode – was dwindle)
#   Hyprland WS 15 → scroll WS 6  (horizontal mode – was dwindle)
#   Hyprland WS 16 → scroll WS 7  (vertical mode – was master)
#   Hyprland WS 17 → scroll WS 8  (vertical mode – was master)
#   Hyprland WS 18 → scroll WS 9  (fullscreen layout – was monocle)
#
# TIP: After switching to WS 7 or 8, run `set_mode v` once (bound to $mod+Ctrl+v).
# The mode persists for that workspace until you change it again.
# TIP: On WS 9, use `fit_size h active proportional` ($mod+f) to maximize each
# window to full width, approximating a monocle/fullscreen layout.
# ══════════════════════════════════════════════════════════════════════════════

# ── Workspace assignments (all to HDMI-A-1) ──────────────────────────────────
workspace 1 output HDMI-A-1
workspace 2 output HDMI-A-1
workspace 3 output HDMI-A-1
workspace 4 output HDMI-A-1
workspace 5 output HDMI-A-1
workspace 6 output HDMI-A-1
workspace 7 output HDMI-A-1
workspace 8 output HDMI-A-1
workspace 9 output HDMI-A-1

# ── Switch to workspace  (Super+1…9) ─────────────────────────────────────────
# Mirrors:  bind = Super, 1, workspace, 10  etc.
bindsym $mod+1 workspace number 1
bindsym $mod+2 workspace number 2
bindsym $mod+3 workspace number 3
bindsym $mod+4 workspace number 4
bindsym $mod+5 workspace number 5
bindsym $mod+6 workspace number 6
bindsym $mod+7 workspace number 7
bindsym $mod+8 workspace number 8
bindsym $mod+9 workspace number 9

# ── Move focused window to workspace  (Super+Shift+1…9) ──────────────────────
# Mirrors:  bind = Super+Shift, 1, movetoworkspace, 10  etc.
bindsym $mod+Shift+1 move container to workspace number 1
bindsym $mod+Shift+2 move container to workspace number 2
bindsym $mod+Shift+3 move container to workspace number 3
bindsym $mod+Shift+4 move container to workspace number 4
bindsym $mod+Shift+5 move container to workspace number 5
bindsym $mod+Shift+6 move container to workspace number 6
bindsym $mod+Shift+7 move container to workspace number 7
bindsym $mod+Shift+8 move container to workspace number 8
bindsym $mod+Shift+9 move container to workspace number 9

# ── Relative workspace switching  (Ctrl+Alt+←/→) ────────────────────────────
# Mirrors:  binde = Ctrl+Alt, right/left, workspace, +1/-1
bindsym Ctrl+Alt+Right workspace next
bindsym Ctrl+Alt+Left  workspace prev

# ── Bracket navigation  (Super+[  /  Super+]) ────────────────────────────────
# focus next/prev non-empty workspace on the current output
# Mirrors:  bind = Super, bracketright/left, workspace, e+1/e-1
bindsym $mod+bracketright workspace next_on_output
bindsym $mod+bracketleft  workspace prev_on_output

# Move window to adjacent workspace
# Mirrors:  bind = Super+Shift, bracketright/left, movetoworkspace, +1/-1
bindsym $mod+Shift+bracketright move container to workspace next; workspace next
bindsym $mod+Shift+bracketleft  move container to workspace prev; workspace prev

# ── Workspace split (scroll-specific) ────────────────────────────────────────
# Shows two workspaces side-by-side on ultra-wide monitors.
# No Hyprland equivalent; a scroll bonus feature.
bindsym $mod+Shift+Tab workspace split
