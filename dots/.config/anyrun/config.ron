Config(
  x: Fraction(0.500000),
  y: Fraction(0.060000),
  width: Fraction(0.400000),
  height: Absolute(0),
  margin: 0,
  hide_icons: false,
  ignore_exclusive_zones: false,
  layer: Overlay,
  hide_plugin_info: true,
  close_on_click: true,
  show_results_immediately: false,
  max_entries: Some(15),
  plugins: ["libapplications.so","librink.so","libshell.so","libstdin.so","libtranslate.so","libsymbols.so"],
)
