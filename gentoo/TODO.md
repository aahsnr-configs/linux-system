- [ ] Use `ghost-router.html` skeleton to write the `README.html`

- [ ] Where to utilize tpm2

- [ ] Secureboot questions - read the whole README.md

- [ ] Write a python script to automatically audit any ebuild from non-official gentoo overlays for security purposes. The only overlay that should be not be audited is the guru overlay.

- [ ] Study the attached README.md file containing the instructions for setting up my hardened Gentoo System. And the attached minimal-config.txt file contains my current kernel config for cachyo-sources. Audit the attached kernel-config-analysis.md making sure it aligns with all the features from the attached README.md file (read this README.md file before performing this task.) I am using sys-kernel/cachyos-sources from CachyOS-kernels overlay and using ananicy-cpp with ananicy-cachyos-rules. Make sure that final kernel config takes that into configuration. Furthermore, unlike kenrel sources packages, when  I first install sys-kernel/cachyos-sources, there is an already existing .config file with the kernel config. The kernel-config-analysis.md did not mention how to do deal with this existing configuration before going about with all the instructions from Section 6 of kernel-config-analysis.md markdown file. I know that my 1st step is to copy over my initial minimal-config.txt to .config /usr/src/config but I want to take care of this existing .config before doing anything. Next step is to `make LLVM=1 olddefconfig`. But In step 2 of section 6, the bash script itself runs `make LLVM=1 olddefconfig` again. Does that mean I run `make LLVM=1 olddefconfig` twice. And where exactly should I run this apply-hardened-config.sh bash script. Step 3 first says to cd to /usr/src/linux but then immediately goes on to execute bash /tmp/appply-hardened-config.sh. Furthermore, in step 4 of Section 6, I am asked to verify all the config options and it mentions that olddefconfig might have or can revert some config option. If I am  understanding this correctly, then is `make olddefconfig` the correct command to use at all. Furthermore, wouldn't it be a good idea to run `make localmodconfig` somewhere along all these steps? Then there are the final important points I want to make. I don't see any kernel config options for firewalld, networkmanager, may zstd is there, then I don't know dracut kernel config optgion for tpm, nvidia and intel microode. Also shouldn't there be explicit config options for secureboot and uiki. The kernel-config-analysis.md markdown file seems to be more invested in hardening the kernel config instead of making the kernel work as intended. Address all these issues in the manner they are intended. Rewrite the kernel-config-analysis.md making sure everything I have discussed has been addressed and fixed. Also make sure my kernel has all the default config options needed for it to function properly.

- [ ] In Section 21 of  my big beautiful README.md file, there is a section about Supply Chain Monitoring. I need to add too more things: I will be using bash scripts to build, install and update apparmor and apparmor-related packages; there will also be packages installed pixi, uv and pipx. Furthermore, there are the texlive packages installed manually from source using texlive rolling distribution manual installation guide since the gentoo texlive packages are very out of date. I will also update texlive packages using tlmgr. For all these cases what can I do about supply chain monitoring as well as hardening the supply chain monitoring. Search the web and think longer for these tasks.
